apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: rook-ceph-dashboard-password
  namespace: storage
spec:
  data:
    - remoteRef:
        key: rook-ceph
      secretKey: password
      sourceRef:
        storeRef:
          kind: ClusterSecretStore
          name: bitwarden
