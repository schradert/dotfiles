apiVersion: v1
kind: Service
metadata:
  name: windmill-mcp
  namespace: dotfiles
spec:
  ports:
    - port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    app.kubernetes.io/name: windmill-mcp
  sessionAffinity: ClientIP
  type: ClusterIP
