apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/instance: descheduler
    app.kubernetes.io/managed-by: Helm
    app.kubernetes.io/name: descheduler
  name: descheduler
  namespace: cicd
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/instance: descheduler
      app.kubernetes.io/name: descheduler
  template:
    metadata:
      annotations:
        checksum/config: 798a1210bd62ae2bb008ce6f0f89715053ddf37132d53773acdc43e7346fb8a1
      labels:
        app.kubernetes.io/instance: descheduler
        app.kubernetes.io/name: descheduler
    spec:
      containers:
        - args:
            - --policy-config-file=/policy-dir/policy.yaml
            - --descheduling-interval=5m
            - --v=3
            - --leader-elect=true
          command:
            - /bin/descheduler
          image: registry.k8s.io/descheduler/descheduler:v0.32.2
          imagePullPolicy: IfNotPresent
          livenessProbe:
            failureThreshold: 3
            httpGet:
              path: /healthz
              port: 10258
              scheme: HTTPS
            initialDelaySeconds: 3
            periodSeconds: 10
          name: descheduler
          ports:
            - containerPort: 10258
              protocol: TCP
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 500m
              memory: 256Mi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
            privileged: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 1000
          volumeMounts:
            - mountPath: /policy-dir
              name: policy-volume
      priorityClassName: system-cluster-critical
      serviceAccountName: descheduler
      volumes:
        - configMap:
            name: descheduler
          name: policy-volume
