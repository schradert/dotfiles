apiVersion: volsync.backube/v1alpha1
kind: ReplicationSource
metadata:
  name: volsync--immich--immich-server-dragonfly-src
  namespace: dotfiles
spec:
  restic:
    copyMethod: Direct
    moverSecurityContext:
      fsGroup: 101
    pruneIntervalDays: 8
    repository: volsync--immich--immich-server-dragonfly
    retain:
      daily: 1
      monthly: 1
      weekly: 1
      yearly: 1
  sourcePVC: immich-server-dragonfly
  trigger:
    schedule: 0 4 * * *
