#+begin_src emacs-lisp :noweb-ref init
(doom! :lang (latex +cdlatex +fold +lsp)
       :tools biblio)
#+end_src

#+begin_src emacs-lisp :tangle no
(setq +latex-viewers '(zathura))
;; TODO how can I integrate reftex for bibliography?
;; (setq reftex-default-bibliography "/your/bib/file.bib")
;; (setq! citar-bibliography "/your/bib/file.bib"
;;        citar-library-paths '("/library/files/")
;;        citar-notes-paths '("/notes/files/"))
;; TODO does this conflict too much with yasnippet?
(map! :map cdlatex-mode-map :i "TAB" #'cdlatex-tab)
#+end_src
